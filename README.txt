Use the AST visualiser by copying the file to your script's directory, importing it, then calling:
visualise(NODE)
where NODE is a Python AST node.

Scroll the canvas by either dragging with the mouse, using WASD or arrow keys and zoom with the mouse wheel.
